pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon

        //Adicionando a biblioteca de Sliders e Calendar
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "OrganizzeBird"
include ':app'
